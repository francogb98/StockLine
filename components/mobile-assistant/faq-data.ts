// Kept for backward compatibility - no longer used as primary UI
export const FAQ_ITEMS: { id: string; question: string; answer: string }[] = []

export function getFAQResponse(_faqId: string): undefined {
  return undefined
}
